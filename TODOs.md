- [x] add the two missing metrics to the main doc comment in `src/lib.rs`: `kopia_snapshot_source_parse_errors` and `kopia_snapshot_timestamp_parse_errors_total`
    - can verify list of metrics by searching for `const NAME` in `src/metrics/` rust source files
